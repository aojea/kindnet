# Copyright 2025 Antonio Ojea
# SPDX-License-Identifier: Apache-2.0

run:
  timeout: 30m
  tests: false # TODO lint tests too

linters:
  disable-all: true
  enable:
    - gocritic
    - gosimple
    - govet
    - errcheck
    - ineffassign
    - staticcheck
    - stylecheck
    - typecheck

formatters:
  disable-all: true
  enable:
    - goimports
    - gofmt